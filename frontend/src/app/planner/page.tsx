import { PlannerPageClient } from "./PlannerPageClient";

/**
 * Primary dashboard: WHOOP-inspired dark layout, responsive to desktop while staying mobile-first.
 */
export default function PlannerPage() {
  return <PlannerPageClient />;
}
