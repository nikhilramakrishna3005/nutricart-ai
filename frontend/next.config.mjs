/** @type {import('next').NextConfig} */
const nextConfig = {
  // Optional: proxy API in dev — uncomment if you prefer same-origin /api
  // async rewrites() {
  //   return [{ source: "/api/:path*", destination: "http://127.0.0.1:8000/:path*" }];
  // },
};

export default nextConfig;
